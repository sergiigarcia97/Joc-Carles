window.onload = function(){
      //haig de repassaro, de moment ho deixem
      /*
      $(".controlAudio").click(function(){
          var src = $(this).attr('src');

          // si no es fa el else, no es podria tornar a encender
          var canviarsrc;
          var controlAudio = document.getElementById("controlAudio");
          if(src=='img/speakon.png'){
              canviarsrc = 'img/speakoff.png';
              $('#controlAudio').trigger("play");
          }else{
              canviarsrc = 'img/speakon.png';
              $('#controlAudio').trigger("pause");
          }
          $(this).attr('src', canviarsrc );
      });
      */
}
